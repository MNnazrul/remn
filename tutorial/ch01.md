### অধ্যায় ১: সেটআপ


আহা, ধাপ ১! একদম নতুনভাবে শুরু করা কি দারুণ না? একেবারে সাদা ক্যানভাস, আর তুমি বেছে নিচ্ছো সেই একটিমাত্র ইট — যার উপর দাঁড়িয়ে তুমি গড়বে তোমার বিশাল প্রাসাদ!

কিন্তু দুর্ভাগ্যবশত, যখন তুমি একটি কম্পিউটার প্রোগ্রাম বানাও, তখন ধাপ ১ অনেকটা জটিল ও বিরক্তিকর হয়ে যায়। কারণ, তোমাকে নিশ্চিত করতে হয় যে তুমি যেই প্রোগ্রামিং ভাষা ব্যবহার করছো তার জন্য তোমার ডেভেলপমেন্ট এনভায়রনমেন্ট ঠিকভাবে সেটআপ হয়েছে কিনা। এছাড়াও, তোমাকে জানতে হয় কিভাবে সেই পরিবেশে তোমার প্রোগ্রাম কম্পাইল এবং রান করতে হয়।

সৌভাগ্যবশত, Rust-এর ডেভেলপমেন্ট এনভায়রনমেন্ট আমাদের জন্য প্রায় সবকিছুই করে দেয়। তাই তোমার দরকার শুধু একটি টেক্সট এডিটর[1] এবং Rust।
এই টিউটোরিয়ালটি Mac-এ লেখা হয়েছে, তবে এটি Windows এবং Linux-এও কাজ করবে।

---

### Rust কীভাবে ইন্সটল করবেন

Rust ইন্সটল করার জন্য আমরা **rustup** ব্যবহার করবো, যা Rust-এর বিভিন্ন ভার্সন এবং সংশ্লিষ্ট টুলগুলো পরিচালনা করে।

Rust-এর সঙ্গে একটি অসাধারণ ফ্রি বই আসে — **The Rust Programming Language**। আমি যখনই মনে করবো যে বইটি কোনো বিষয়ে আমাকে থেকে ভালোভাবে ব্যাখ্যা করেছে, তখনই সেই বইয়ের লিঙ্ক দেব।

Rust ইন্সটল করার জন্য, তোমার প্ল্যাটফর্ম অনুযায়ী **Chapter 1.1: Installation** অংশটি অনুসরণ করো।
বর্তমানে Mac-এ এটি এভাবে কাজ করে:
টার্মিনাল খুলো (Spotlight-এ “Terminal” লিখে সার্চ করো) এবং নিচের কমান্ডটি লিখো:

```
curl --proto '=https' --tlsv1.2 https://sh.rustup.rs -sSf | sh
```

ইনস্টলার কিছু অপশন দেখাবে এবং ডিফল্ট সেটিং নিতে চাইলে শুধু **Enter** চাপলেই হবে।
ইনস্টলেশন শেষে তুমি এই উৎসাহব্যঞ্জক বার্তাটি পাবে:

> Rust is installed now. Great!

এখন তোমার টার্মিনাল **রিস্টার্ট** করো, এবং নিচের কমান্ডটি টাইপ করো:

```
rustc --version
```

তুমি তোমার ইন্সটল করা ভার্সনের তথ্য দেখতে পাবে।
যদি কিছু না দেখাও, তাহলে **Installation guide**-এ গিয়ে সমস্যা সমাধান করো।

এই টিউটোরিয়ালে আমরা টার্মিনাল অনেক ব্যবহার করবো, তাই এটি খোলা রাখাই ভালো।

---

### main() ফাংশন

এখন `hecto.rs` নামে একটি নতুন ফাইল তৈরি করো এবং এর মধ্যে একটি `main()` ফাংশন লিখো।
(hecto হলো সেই টেক্সট এডিটর যার কোড আমরা তৈরি করবো।)

```rust
fn main() {
    return;
}
```

Rust-এ, তোমাকে সব executable কোড **function**-এর ভিতরে লিখতে হয়।
`main()` ফাংশনটি Rust-এ বিশেষ, কারণ এটি প্রোগ্রামের **ডিফল্ট এন্ট্রি পয়েন্ট**।
যখন তুমি `main()` ফাংশন থেকে **return** করো, তখন প্রোগ্রামটি বন্ধ হয়ে যায় এবং নিয়ন্ত্রণ **অপারেটিং সিস্টেমের** হাতে ফিরে যায়।[2]

Rust একটি **compiled language**।
অর্থাৎ, তোমাকে তোমার প্রোগ্রামটিকে Rust কম্পাইলারের মাধ্যমে **একটি এক্সিকিউটেবল ফাইলে** রূপান্তর করতে হবে।
এরপর সেই এক্সিকিউটেবল ফাইলটি তুমি টার্মিনাল থেকে অন্য প্রোগ্রামের মতোই চালাতে পারবে।

`hecto.rs` কম্পাইল করতে টার্মিনালে লিখো:

```
rustc hecto.rs
```

যদি কোনো ত্রুটি না থাকে, তাহলে এটি একটি এক্সিকিউটেবল তৈরি করবে যার নাম হবে `hecto`।
প্রোগ্রামটি চালানোর জন্য টাইপ করো:

```
./hecto
```

এবং **Enter** চাপো।
এই প্রোগ্রামটি কোনো আউটপুট দেখাবে না।[3]


### কার্গো দিয়ে কম্পাইল করা

তত্ত্ব অনুযায়ী, `rustc`-ই যথেষ্ট `hecto` তৈরি করার জন্য।
কিন্তু বাস্তবে, Rust কিছু অতিরিক্ত সুবিধাজনক টুলসহ আসে, যা তোমার ডেভেলপমেন্ট প্রক্রিয়াকে আরও সহজ — এবং পরে দেখা যাবে, আরও **নিরাপদ** করে তোলে।

Rust-এর সঙ্গে একটি প্রোগ্রাম আসে যার নাম **cargo**।
যদি তুমি JavaScript-এর ইকোসিস্টেমের সাথে পরিচিত হও, তবে **cargo**-কে Rust-এর **npm** হিসেবে ভাবা যেতে পারে।
এটি তোমার **dependencies** (প্রয়োজনীয় প্যাকেজসমূহ) ম্যানেজ করে, কোড কম্পাইল করে এবং আরও অনেক কাজ করে।

Rust ঠিকভাবে ইনস্টল করা থাকলে, নিচের কমান্ডটি চালিয়ে তুমি কার্গোর ভার্সন দেখতে পারো:

```
cargo --version
```

---

### hecto প্রজেক্ট শুরু করা

এখন আমরা `hecto` প্রজেক্ট শুরু করবো কার্গো ব্যবহার করে।
প্রথমে আগের তৈরি করা `hecto.rs` ফাইল এবং `hecto` এক্সিকিউটেবলটি মুছে ফেলো:

```
rm hecto.rs hecto
```

এখন যে ফোল্ডারে তুমি `hecto` তৈরি করতে চাও, সেখানে নিচের কমান্ডটি চালাও:

```
cargo init hecto --vcs none
```

এখানে `--vcs none` অপশনটি ব্যবহার করলে প্রজেক্টটি **git ছাড়া** ইনিশিয়ালাইজ হবে।
তুমি যদি git ব্যবহার করতে চাও, তাহলে এই অপশনটি বাদ দিতে পারো (তবে সেক্ষেত্রে তোমার সিস্টেমে git ইনস্টল থাকতে হবে)।

সফলভাবে তৈরি হলে কার্গো নিচের বার্তা দেখাবে:

> Created binary (application) package

এটি `hecto` নামে একটি নতুন ফোল্ডার তৈরি করবে, যেখানে কিছু ফাইল থাকবে।
সবচেয়ে গুরুত্বপূর্ণ ফাইলটি হলো `src` ফোল্ডারের ভিতরে থাকা `main.rs`।

---

### main.rs দেখা

`main.rs` ফাইলটি খুললে তুমি দেখবে যে এটি ইতিমধ্যেই একটি `main()` ফাংশন ধারণ করে।
শুধু তাকিয়েই বোঝা যায়, এই প্রোগ্রামটি সেই প্রাচীন প্রথা অনুসরণ করে —
অর্থাৎ এটি **"Hello, World!"** প্রিন্ট করে এবং তারপর প্রস্থান করে।

---

### কার্গো দিয়ে প্রোগ্রাম কম্পাইল করা

প্রোগ্রামটি কার্গো দিয়ে কম্পাইল করতে হলে নিশ্চিত হও যে তুমি এখন `hecto` ফোল্ডারে আছো।
(যদি না থাকো, তাহলে `cargo init hecto` করার পরে নিচের কমান্ড চালাও:)

```
cd hecto
```

তারপর কমান্ড চালাও:

```
cargo build
```

`cargo` এখানে `rustc`-এর মতো নীরব নয় — এটি কিছু আউটপুট দেখাবে, যেমন:

```
   Compiling hecto v0.1.0 (/Users/pflenker/repositories/hecto)
    Finished dev [unoptimized + debuginfo] target(s) in 12.36s
```

এই কমান্ডটি চালানোর পর আরও কিছু ফাইল তৈরি হবে (যেগুলো আমরা পরে দেখব)।
এর মধ্যে একটি এক্সিকিউটেবল তৈরি হবে `target/debug` ফোল্ডারে, নাম `hecto`।

এখন প্রোগ্রামটি চালাও এবং দেখো এটি সত্যিই “Hello, World!” প্রিন্ট করে কিনা:

```
./target/debug/hecto
```


### কার্গোর অতিরিক্ত ফাইলগুলো বোঝা (এবং এর ফিচারসমূহ)

এখন আমাদের `hecto` ডিরেক্টরির মধ্যে বেশ কিছু নতুন জিনিস রয়েছে (এর কিছু লুকানো থাকতে পারে, এবং তোমার কম্পিউটারে এগুলোর অবস্থান বা নাম কিছুটা ভিন্ন হতে পারে):

* **`src`** নামে একটি ফোল্ডার, যার মধ্যে আছে `main.rs`
* **`Cargo.toml`** এবং **`Cargo.lock`** নামে দুটি ফাইল
* **`target`** নামে একটি ফোল্ডার, যার ভিতরে কিছু লুকানো ফাইল এবং **`debug`** নামে আরও একটি ফোল্ডার রয়েছে।
  `debug` ফোল্ডারের ভেতরে আরও কিছু ফাইল ও ফোল্ডার আছে — এবং সেখানে আমাদের এক্সিকিউটেবল **`hecto`** রয়েছে।

এখন প্রশ্ন হলো — এই অতিরিক্ত জিনিসগুলো আসলে কী কাজে লাগে? চল, একে একে দেখি।

---

### 🧩 The `src` ফোল্ডার

`src` ফোল্ডারেই আমাদের সব **source code ফাইল** থাকবে।
এই মুহূর্তে এখানে শুধু একটি ফাইল — `main.rs` — আছে।
তবে খুব শিগগিরই আমরা আরও ফাইল যোগ করবো।

---

### 📦 Cargo.toml এবং Cargo.lock

**`Cargo.toml`** ফাইলটি একটি **TOML** (Tom’s Obvious, Minimal Language) ফরম্যাটে লেখা কনফিগারেশন ফাইল।
এটি কম্পাইলারকে জানায়, প্রজেক্টটি কেমন ধরনের এবং কীভাবে এটি কম্পাইল করতে হবে।
আমরা কয়েক অনুচ্ছেদ পরেই এই ফাইলটি বিস্তারিতভাবে দেখবো।

এছাড়াও, **cargo** আমাদের জন্য **dependency management**-এর কাজও করে।
অর্থাৎ, যদি আমাদের প্রজেক্টে বাইরের লাইব্রেরি দরকার হয়, সেগুলোর তথ্য **Cargo.toml**-এ সংরক্ষিত থাকবে।

অন্যদিকে, **`Cargo.lock`** ফাইলটি নিশ্চিত করে যে এই নির্ভরশীলতাগুলো (dependencies)
সব পরিবেশে একই সংস্করণে থাকে — যাতে প্রজেক্ট চালানোর সময় কোনো অসঙ্গতি না হয়।

এখনই এ নিয়ে চিন্তা করার দরকার নেই —
আমরা পরবর্তী অধ্যায়ে এই বিষয়টি বিস্তারিতভাবে শিখবো।


### 🧱 বিল্ড টার্গেটস (Build Targets)

এখন চল দেখি `target` ফোল্ডার এবং এর ভেতরের বিষয়গুলো।

**cargo** একাধিক ধরণের **build target** সমর্থন করে।
এর মধ্যে **ডিফল্ট** টার্গেট হলো **debug**, যার মানে হলো এই বিল্ডটি মূলত **ডেভেলপারদের জন্য**,
না যে এটি সরাসরি **চূড়ান্ত ব্যবহারকারীদের (end users)** হাতে দেওয়া হবে।

অন্যদিকে, আরেকটি টার্গেট হলো **release**, যা বিপরীত —
এটি হলো সেই সংস্করণ যা ব্যবহারকারীদের দেওয়া হয়,
অর্থাৎ **final product** হিসেবে প্রকাশ করার জন্য তৈরি করা হয়।

---

### 🚀 একটি Release বিল্ড তৈরি করা

একটি release সংস্করণ তৈরি করতে নিচের কমান্ডটি চালাও:

```
cargo build --release
```

এই কমান্ডটি চালানোর পর তুমি দেখবে যে `target` ফোল্ডারের ভিতরে
আরও একটি নতুন ফোল্ডার তৈরি হয়েছে, নাম **release**।

---

### 🎯 “Executable is targeted at someone” — এর মানে কী?

তুমি ভাবতে পারো, “এক্সিকিউটেবল কাকে লক্ষ্য করে তৈরি হলো বলতে কী বোঝায়?
এটা তো শুধু ‘Hello, World!’ প্রিন্ট করে, তাই না?” 🤔

ঠিকই বলেছো, সেটাই করে।
তবে এখানে মূল পার্থক্যটি হলো **Rust কম্পাইলার কিভাবে ডেভেলপার এবং ব্যবহারকারীদের জন্য কোড পরিচালনা করে।**

---

### ⚙️ Debug বনাম Release: মূল পার্থক্য

1. **ত্রুটি (Error) ব্যবস্থাপনা**
   Rust এমনভাবে তৈরি করা হয়েছে যাতে এটি ডেভেলপার এবং ব্যবহারকারীর অভিজ্ঞতাকে আলাদা করে বিবেচনা করে।
   কিছু ধরণের প্রোগ্রামিং ত্রুটির ক্ষেত্রে Rust ধরে নেয়, “তুমি এটা ইচ্ছে করে করোনি।” 😅
   তাই **debug** বিল্ডে এমন ত্রুটি ঘটলে প্রোগ্রাম **ক্র্যাশ** করে —
   যেন তুমি বুঝতে পারো, কোথাও গুরুতর ভুল হয়েছে।

   কিন্তু **release** বিল্ডে, Rust এই ধরনের ত্রুটি থেকে **রিকভার (recover)** করার চেষ্টা করে,
   যাতে প্রোগ্রাম চলতে থাকে (যদিও ভুল ফলাফল দিতে পারে)।
   কারণ, **ব্যবহারকারীর কাছে ক্র্যাশ হওয়া ফলাফলের চেয়ে খারাপ।**

---

2. **অপ্টিমাইজেশন (Optimization)**
   Rust কম্পাইলার **release** বিল্ডে কোডকে দ্রুত চালানোর জন্য নানা ধরণের **অপ্টিমাইজেশন** করে।
   তবে এই প্রক্রিয়াটি সময়সাপেক্ষ, তাই **কম্পাইল টাইম ধীর হয়ে যায়।**

   * **ডেভেলপাররা** ঘন ঘন কম্পাইল করে, তাই তাদের জন্য **দ্রুত কম্পাইলেশন** বেশি গুরুত্বপূর্ণ।
   * **ব্যবহারকারীরা** কখনও কোড কম্পাইল করে না, তাদের জন্য **দ্রুত প্রোগ্রাম চলা** গুরুত্বপূর্ণ।

   এজন্যই debug বিল্ডে অপ্টিমাইজেশন বন্ধ থাকে (দ্রুত কম্পাইলের জন্য),
   আর release বিল্ডে এটি চালু থাকে (দ্রুত রানটাইম পারফরম্যান্সের জন্য)।

---

3. **ডিবাগ তথ্য (Debug Information)**
   Debug বিল্ডে Rust অতিরিক্ত কিছু তথ্য যোগ করে, যা **বাগ খুঁজে বের করতে সাহায্য করে।**
   এই তথ্যগুলো শুধুমাত্র ডেভেলপারদের জন্য দরকারি,
   তাই **release বিল্ডে এগুলো বাদ দেওয়া হয়** —
   ফলে প্রোগ্রাম আরও হালকা ও দ্রুত হয়।

---

সুতরাং সংক্ষেপে বলা যায় —

* **Debug build** = ডেভেলপারদের জন্য, দ্রুত কম্পাইল হয়, বেশি তথ্য দেয়।
* **Release build** = ব্যবহারকারীদের জন্য, দ্রুত রান করে, ছোট আকারে থাকে।

### 🧠 কার্গোর বার্তা থেকে কী শেখা যায়

কার্গো আমাদের বিল্ড সম্পর্কেও তথ্য দেয়।
উদাহরণস্বরূপ, **debug বিল্ড** শেষ হলে এটি এমন একটি লাইন দেখায়:

```
Finished dev [unoptimized + debuginfo] target(s) in 0.04s
```

এটি বোঝায় যে এই বিল্ডে **অপ্টিমাইজেশন করা হয়নি** এবং এতে **ডিবাগ ইনফরমেশন** অন্তর্ভুক্ত রয়েছে।

অন্যদিকে, **release বিল্ড** শেষ হলে দেখা যায়:

```
Finished release [optimized] target(s) in 0.04s
```

এটি বোঝায় যে এতে **কোনো ডিবাগ তথ্য নেই**, এবং এটি একটি **অপ্টিমাইজড বিল্ড**।

---

### 🔍 Debug তথ্য কাছ থেকে দেখা

আমরা এখন এই “debug info” ঠিক কী বোঝায় তা একটু কাছ থেকে দেখবো।
তবে আমাদের নতুন প্রিয় `hecto` কোড নষ্ট না করে,
আমরা ব্যবহার করবো **Rust Playground**,
যেটি অনলাইনে Rust কোড চালানোর একটি টুল — এবং এটি আগে থেকেই একটি “Hello, World!” প্রোগ্রাম দেয়।

---

### 🌐 Rust Playground-এ পদক্ষেপ

1. [Rust Playground](https://play.rust-lang.org/) এ যাও।

2. উপরের বাম দিকে কিছু বোতাম দেখবে —
   **Debug**, **Stable**, এবং তারপরে তিনটি বিন্দু (`...`)।
   সেখানে ক্লিক করে **“Enable backtrace”** চালু করো।

3. এখন কোডটি নিচের মতো করে পরিবর্তন করো:

   ```rust
   fn main() {
       panic!("Hello, World");
   }
   ```

   এখানে `panic!` একটি **ক্র্যাশ ট্রিগার করে** — সহজভাবে বললে, এটি প্রোগ্রামটি ইচ্ছাকৃতভাবে বন্ধ করে দেয়।

4. **“Run”** বোতামে ক্লিক করে প্রোগ্রামটি চালাও।
   তারপর **“Debug”** থেকে **“Release”** নির্বাচন করে আবার চালাও।

---

### 🧩 আউটপুটে পার্থক্য

#### 🟢 Release বিল্ডের আউটপুট

```
Running `target/release/playground`
thread 'main' panicked at src/main.rs:2:4:
Hello, World
stack backtrace:
   0: rust_begin_unwind
   1: core::panicking::panic_fmt
   2: playground::main
```

#### 🟠 Debug বিল্ডের আউটপুট

```
Running `target/debug/playground`
thread 'main' panicked at src/main.rs:2:4:
Hello, World
stack backtrace:
   0: rust_begin_unwind
             at /rustc/7cf61ebde7b22796c69757901dd346d0fe70bd97/library/std/src/panicking.rs:647:5
   1: core::panicking::panic_fmt
             at /rustc/7cf61ebde7b22796c69757901dd346d0fe70bd97/library/core/src/panicking.rs:72:14
   2: playground::main
             at ./src/main.rs:2:4
   3: core::ops::function::FnOnce::call_once
             at /rustc/7cf61ebde7b22796c69757901dd346d0fe70bd97/library/core/src/ops/function.rs:250:5
```

---

### 🧭 Stack Trace কীভাবে সাহায্য করে

আমরা এখনো “**Stack Trace**” সম্পর্কে বিস্তারিত শিখিনি,
কিন্তু এখান থেকেই দেখা যাচ্ছে যে **debug আউটপুটে অনেক বেশি তথ্য থাকে** —
যেমন কোন ফাংশন কোথা থেকে কল হয়েছে, কোন লাইনে সমস্যা ঘটেছে ইত্যাদি।

“**Call stack**” বলতে বোঝায় —
কোন ক্রমে ফাংশনগুলো একে অপরকে কল করেছে এবং শেষ পর্যন্ত কোথায় গিয়ে প্রোগ্রামটি ক্র্যাশ করেছে।

এই তথ্যগুলো **ডিবাগ করার সময় অত্যন্ত মূল্যবান**,
কারণ এটি সাহায্য করে **ঠিক কোন অংশে ত্রুটি ঘটেছে তা সঠিকভাবে খুঁজে বের করতে।**


---

### 🧹 `cargo clean`

এই কমান্ডটি **`target` ফোল্ডারটি মুছে ফেলে** (যেখানে সব কম্পাইল করা ফাইল থাকে)।
এটা চালানোর পর, প্রকল্পটি একদম পরিষ্কার অবস্থায় ফিরে যায় — কোনো কম্পাইল করা ফাইল বা ক্যাশ থাকে না।

---

### ⚙️ `cargo build`

যখন তুমি এটি **প্রথমবার চালাও**:

* Cargo তোমার Rust কোড (`main.rs` এবং এর ডিপেন্ডেন্সিগুলো) কম্পাইল করে।
* এটি তোমার এক্সিকিউটেবল ফাইল (যেমন `hecto`) তৈরি করে।
* একইসঙ্গে `target/debug/` ফোল্ডারের মধ্যে অনেক লুকানো ফাইল তৈরি হয় — এগুলোকেই বলে **build artefacts**।

এই artefacts-এর মধ্যে থাকে:

* **Compiled object files (.o)** — আংশিক কম্পাইল করা কোডের ফাইল।
* **Dependency metadata** — কোন ফাইল কখন কম্পাইল হয়েছে, কোন ভার্সন ব্যবহার হয়েছে — এমন তথ্য।
* **Incremental compilation cache** — ভবিষ্যতের বিল্ডগুলো দ্রুত করার জন্য ব্যবহার হয়।

---

### ⚡ আবার `cargo build` চালালে

দ্বিতীয়বার চালালে:

* Cargo তোমার সোর্স ফাইলগুলোর (যেমন `main.rs`) সময় চেক করে।
* যদি কিছু পরিবর্তন না হয়ে থাকে, তবে এটি **আবার কম্পাইল করে না**।
* বরং আগের কম্পাইল করা ফাইলগুলো **ক্যাশ থেকে পুনরায় ব্যবহার করে**।

এটিকেই বলে **incremental compilation**, যা Cargo-কে বড় প্রকল্পেও দ্রুত কাজ করতে সাহায্য করে।

---

### 🏗️ কেন এটা দরকার

যখন তোমার প্রকল্প বড় হবে (অনেক ফাইল ও ডিপেন্ডেন্সি থাকবে):

* প্রতিবার সব কিছু নতুন করে কম্পাইল করা অনেক সময় লাগবে।
* Cargo এই build artefacts ব্যবহার করে শুধু **পরিবর্তিত অংশগুলোই কম্পাইল করে**।
* ফলে কাজের গতি অনেক বেড়ে যায়।

---

✅ **সংক্ষেপে:**

| কমান্ড                     | কাজ                                                                       |
| -------------------------- | ------------------------------------------------------------------------- |
| `cargo clean`              | সব build artefacts মুছে ফেলে (পরের বার পুরো নতুনভাবে কম্পাইল হবে)         |
| `cargo build` (প্রথমবার)   | সব কিছু কম্পাইল করে এবং artefacts তৈরি করে                                |
| `cargo build` (দ্বিতীয়বার) | আগের artefacts ব্যবহার করে; পরিবর্তন না থাকলে কিছুই পুনরায় কম্পাইল করে না |


---

## 🔧 **Compiling and Running (কম্পাইল ও রান করা)**

তুমি প্রায়ই তোমার প্রোগ্রাম **কম্পাইল করে সঙ্গে সঙ্গে চালাতে (run)** চাইবে।
Rust-এ এজন্য আলাদা করে দুটি ধাপ করতে হয় না —
Cargo এক কমান্ডেই দুটো কাজ করে দেয়:

```bash
cargo run
```

তুমি এখন `main.rs` ফাইলের ভেতরে থাকা `"Hello, World"` স্ট্রিংটি পরিবর্তন করে দেখো, যেমন `"Hi, Rust!"` দাও।

তারপর চালাও:

```bash
cargo run
```

এখন এটি তোমার কোড **কম্পাইল করবে ও চালাবে**।
টার্মিনালে তুমি নতুন স্ট্রিংটি দেখতে পাবে।
তারপর আবার `"Hello, World"` লিখে দাও, `cargo run` চালাও,
এবং নিশ্চিত হও যে আগের মতোই `"Hello, World"` ফিরে এসেছে।

---

## 👀 **Code Review (কোড রিভিউ)**

এই ধাপে আমরা একসঙ্গে কোড দেখব।
টিউটোরিয়ালের লেখক একটি GitHub লিংক দিয়েছে,
যেখানে তুমি দেখতে পারবে **আগের ধাপ থেকে এই ধাপ পর্যন্ত কী পরিবর্তন হয়েছে**।

GitHub-এ গিয়ে তুমি দেখবে:

* প্রতিটি পরিবর্তিত লাইনে ব্যাখ্যা দেওয়া আছে।
* সেই ব্যাখ্যা দেখে তুমি বুঝতে পারবে, কেন ও কীভাবে কোডে পরিবর্তন করা হয়েছে।
* তোমার GitHub অ্যাকাউন্ট থাকলে তুমি সরাসরি প্রশ্নও করতে পারবে বা মন্তব্য করতে পারবে।

প্রথম দিকে হয়তো টিউটোরিয়াল আর GitHub-এর মধ্যে বারবার যাওয়া ক্লান্তিকর লাগবে।
কিন্তু পরের দিকে লেখক **আগে অ্যাসাইনমেন্ট দেবে**,
তারপর তুমি নিজে চেষ্টা করবে,
এরপর দেখবে লেখক কীভাবে সমাধান করেছেন।
তখন তোমাকে GitHub-এ কম যেতেও হবে।

---

## 🔗 **Links (লিংকের নিয়ম)**

টিউটোরিয়ালে অনেক লিংক থাকবে।
লেখকের নিয়ম হলো:

* যেসব লিংকে গুরুত্বপূর্ণ ব্যাখ্যা আছে, সেগুলো **GitHub commit** বা **এই টেক্সটের মধ্যে** থাকবে।
* অন্য লিংকগুলো শুধুমাত্র **অতিরিক্ত বা ঐচ্ছিক তথ্যের** জন্য —
  আগ্রহ না থাকলে তুমি এগুলো বাদ দিতে পারো।

---

## 🧭 **Wrap-up and Outlook (এই অধ্যায়ের সারসংক্ষেপ ও পরবর্তী দিক)**

এই অধ্যায়ে আমরা যা শিখেছি 👇

✅ Rust ইনস্টল করেছি।
✅ একটি নতুন প্রজেক্ট তৈরি করেছি (`cargo new hecto`)।
✅ কোড কীভাবে **কম্পাইল** করতে হয় — হাতে (`rustc`) ও Cargo দিয়ে — তা শিখেছি।
✅ Cargo এবং তার তৈরি ফাইলগুলো (`Cargo.toml`, `Cargo.lock`, `target/`) বুঝেছি।
✅ কোড কীভাবে **clean** করতে হয় (`cargo clean`) সেটাও দেখেছি।
✅ Rust Playground চিনেছি — যেখানে আলাদা করে ছোট কোড এক্সপেরিমেন্ট করা যায়।

এখন আমরা জানি কীভাবে প্রজেক্ট তৈরি করতে হয়,
কীভাবে বিল্ড করতে হয়,
এবং প্রয়োজনে আগের বিল্ড মুছে নতুনভাবে শুরু করতে হয়।

---

